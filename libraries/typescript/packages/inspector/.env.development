# Default for `pnpm dev` — points at local cloud backend.
# Override in `.env.development.local` or export before running.
VITE_MANUFACT_CHAT_URL=http://localhost:8000/api/v1/inspector/chat/stream
# Vite dev server port (also: `pnpm dev -- --port 3005`)
INSPECTOR_PORT=3000
