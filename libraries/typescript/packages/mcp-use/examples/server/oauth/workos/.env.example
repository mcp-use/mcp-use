# Found in WorkOS Dashboard → API Keys tab
MCP_USE_OAUTH_WORKOS_API_KEY=
# Found in WorkOS Dashboard → Domains tab → AuthKit section
# e.g., "random-text-44-staging.authkit.app"
MCP_USE_OAUTH_WORKOS_SUBDOMAIN=
