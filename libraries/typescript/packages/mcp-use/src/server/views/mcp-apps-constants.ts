/**
 * MCP Apps Extension constants (SEP-1865).
 * Inlined from @modelcontextprotocol/ext-apps to avoid pulling optional @oven/bun.
 *
 * @see https://github.com/modelcontextprotocol/ext-apps
 */

/** Metadata key for associating a UI resource URI with a tool (legacy flat format). */
export const RESOURCE_URI_META_KEY = "ui/resourceUri";

/** MIME type for MCP App UI resources. */
export const RESOURCE_MIME_TYPE = "text/html;profile=mcp-app";
