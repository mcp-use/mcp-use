/**
 * @mcp-use/client — v2 scaffold.
 *
 * Intentionally empty for now. The client surface (MCPClient, connectors,
 * session, task managers, browser OAuth, logging, config, and client-side
 * utils) moves here in Phase 3 of the v2 package split. See MCP-2601.
 */
export {};
