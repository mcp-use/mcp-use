{
  "name": "mcp-use-example-sentry",
  "type": "module",
  "version": "0.0.0",
  "private": true,
  "description": "Example: trace MCP tool calls and report failures with Sentry.",
  "engines": { "node": ">=22.22.2" },
  "scripts": {
    "dev": "mcp-use dev",
    "build": "mcp-use build",
    "start": "mcp-use start",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "verify": "node ../verify-examples.mjs --example=sentry"
  },
  "dependencies": {
    "@sentry/node": "10.63.0",
    "mcp-use": "workspace:*",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^22.20.0",
    "typescript": "^7.0.2",
    "vite": "^8.0.16",
    "vitest": "4.1.11"
  }
}
