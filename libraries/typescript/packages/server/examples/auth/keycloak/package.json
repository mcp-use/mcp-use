{
  "name": "mcp-use-example-auth-keycloak",
  "type": "module",
  "version": "0.0.0",
  "private": true,
  "description": "Example: mcp-use direct Keycloak OAuth resource server.",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "dev": "mcp-use dev",
    "build": "mcp-use build",
    "start": "mcp-use start",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "mcp-use": "workspace:*",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^22.13.0",
    "typescript": "^7.0.2"
  }
}
