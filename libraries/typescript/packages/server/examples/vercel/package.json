{
  "name": "mcp-use-example-vercel",
  "type": "module",
  "version": "0.0.0",
  "private": true,
  "description": "Example: mcp-use on Vercel (serverless) via the web-standard fetch handler from getHandler().",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "mcp-use": "workspace:*",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^22.13.0",
    "typescript": "^7.0.2"
  }
}
