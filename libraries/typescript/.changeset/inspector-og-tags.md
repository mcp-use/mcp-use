---
"@mcp-use/inspector": patch
---

Add Open Graph and Twitter Card meta tags (title, description, image, site) plus a `<meta name="description">` to the inspector HTML shell so links to the hosted inspector render rich previews on Slack, Twitter/X, LinkedIn, Discord, and other platforms. Applies to both the default Vite build and the CDN-shell path.
