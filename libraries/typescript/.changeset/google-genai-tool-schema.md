---
"mcp-use": patch
---

Strip unsupported JSON Schema `propertyNames` from MCP tool definitions when using ChatGoogleGenerativeAI, fixing HTTP 400 errors from the Generative Language API (e.g. inspector chat with Gemini).
