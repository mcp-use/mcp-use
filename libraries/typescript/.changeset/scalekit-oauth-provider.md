---
"mcp-use": minor
---

Add `oauthScalekitProvider` for Scalekit MCP Auth. The verifier accepts environment-root and resource-scoped issuers, binds audience to the `res_…` resource id, and advertises DCR plus CIMD.
