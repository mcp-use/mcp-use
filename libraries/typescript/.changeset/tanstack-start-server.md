---
"mcp-use": minor
"@mcp-use/cli": patch
---

Add `mcp-use/tanstack-start` and `mcp-use/tanstack-start/vite` for mounting MCP servers in TanStack React Start. The Vite plugin isolates view compilation and bundles view assets and skills into the server output. Include a Node/Nitro example with shared React components and MCP tools.
