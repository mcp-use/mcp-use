---
"@mcp-use/client": minor
---

Add opt-in Jev tool selection for code mode and a shared TypeSafe service for a fail-closed MCP tool-result prompt-injection firewall. Preserve keyword discovery by default and expose typed escalation errors.
