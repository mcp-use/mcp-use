---
"mcp-use": minor
---

Add `oauthConvexProvider` at `mcp-use/oauth/convex` for the Convex OAuth Provider component. Takes the mounted origin including its base path, such as `https://example.convex.site/oauth`, which is also the issuer Convex puts in its tokens.
