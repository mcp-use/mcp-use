---
"create-mcp-use-app": patch
---

Install the native v2 MCP Apps skill from the matching release branch, use the standard `.agents/skills` project directory for Codex, resolve stable scaffolds from npm's `latest` tag, and normalize stable internal peer ranges.
