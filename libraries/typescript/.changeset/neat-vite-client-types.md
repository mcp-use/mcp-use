---
"@mcp-use/cli": patch
"create-mcp-use-app": patch
"mcp-use": patch
---

Provide Vite client types through `mcp-use` so generated projects can import CSS, SVG, and other supported assets without maintaining custom declarations or depending directly on Vite.
